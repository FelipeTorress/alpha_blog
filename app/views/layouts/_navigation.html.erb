<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand", id: "logo" %>
  </div> 

  <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Artigos', articles_path, class: "nav-link" %></li>
        <li><%= link_to 'Usuários', users_path, class: "nav-link" %></li>
        <% if logged_in? %>
          <li><%= link_to 'Novo Artigo', new_article_path, class: "nav-link" %></li>
          <li><%= link_to "#{"(Admin)" if current_user.admin?} Perfil [#{current_user.username}] ", user_path(current_user), class: "nav-link" %></li>
          <li><%= link_to 'Deslogar', logout_path, data: { "turbo-method": :delete , "turbo-confirm": "Você tem certeza que deseja sair?"} %></li>
        <% else %>
          <li><%= link_to 'Logar', login_path, class: "nav-link" %></li>
          <li><%= link_to 'Cadastrar-se', signup_path, class: "nav-link" %></li>
        <% end %>
        
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li>
          <form class="navbar-form navbar-left">
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Search">
            </div>
            <button type="submit" class="btn btn-success">Submit</button>
          </form>
        </li>
        </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>